#include <unistd.h>
#include <stdio.h>

int main()
{
    char buffer[100];
    int n;

    printf("Enter characters: ");
    fflush(stdout);

    n = read(0, buffer, 100);

    printf("\nYou entered: ");
    fflush(stdout);

    write(1, buffer, n);

    return 0;
}
